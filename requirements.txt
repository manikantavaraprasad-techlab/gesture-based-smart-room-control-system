# ── Core computer vision + ML ───────────────────────────────────────────────
opencv-python>=4.10.0
numpy>=1.24.0
mediapipe>=0.10.14

# ── Networking / async (WebSocket bridge to HTML visualizer) ────────────────
websockets>=12.0

# ── Optional but recommended for stability / UX ─────────────────────────────
typing-extensions>=4.8.0
